---
paths: "**/*.py, **/pyproject.toml, **/requirements*.txt, **/setup.py, **/setup.cfg"
---

# Python Package Installation

Always run `pip index versions <package>` first to check the latest version, then install with the verified version.
