---
paths: "**/package.json, **/package-lock.json, **/*.js, **/*.ts, **/*.jsx, **/*.tsx"
---

# NPM Package Installation

Always run `npm view <package>` first to check the latest version, then install with the verified version.
